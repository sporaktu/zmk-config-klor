# CRITICAL: Disable NFC to use pins as GPIOs (required for nice_nano_v2)
CONFIG_NFCT_PINS_AS_GPIOS=y

# Bluetooth Configuration - matching working Yellowjacket config
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_BT_MAX_CONN=6
CONFIG_BT_MAX_PAIRED=6

# Disable all extra features for minimal config
# CONFIG_EC11=y
# CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y
# CONFIG_ZMK_DISPLAY=y
# CONFIG_ZMK_RGB_UNDERGLOW=y
# CONFIG_WS2812_STRIP=y

# Note: USB logging disabled - only works on central (left) side
# Enable in klor_left.conf if needed for debugging
# CONFIG_ZMK_USB_LOGGING=y
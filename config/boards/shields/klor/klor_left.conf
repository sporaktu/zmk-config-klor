# ZMK Studio — central (left) only.
# Requires the studio-rpc-usb-uart snippet (build.yaml) and a physical layout
# with a `keys` property (klor.dtsi). Uses more RAM, so keep it off the peripheral.
CONFIG_ZMK_STUDIO=y
